# Line_RN

## 📱 Описание

Line_RN - это мобильное приложение на React Native для просмотра IP-камер систем видеонаблюдения. Приложение позволяет легко подключаться к серверам видеонаблюдения через сканирование QR-кодов, просматривать камеры в реальном времени, работать с архивными записями, а также воспроизводить прямые RTSP потоки от IP-камер.

## ✨ Основные функции

- **QR-сканирование**: Быстрое добавление серверов через сканирование QR-кодов
- **Управление серверами**: Добавление, редактирование и удаление серверов видеонаблюдения
- **RTSP потоки**: Прямое подключение и просмотр RTSP потоков от IP-камер
- **Live-просмотр**: Просмотр камер в реальном времени с адаптивным интерфейсом
- **Архивные записи**: Доступ к архиву с удобным timeline интерфейсом
- **Полноэкранный режим**: Просмотр камер в полном экране с поддержкой зума
- **VLC плеер**: Высококачественное воспроизведение видеопотоков с использованием VLC
- **Адаптивный дизайн**: Поддержка портретной и альбомной ориентации
- **Темная тема**: Современный темный интерфейс для комфортного просмотра
- **Локальное хранение**: Автоматическое сохранение настроек серверов и RTSP потоков

## 🛠 Технологии

- **React Native**: 0.79.5
- **React**: 19.0.0
- **Expo**: ~53.0.20
- **TypeScript**: ~5.8.3
- **React Navigation**: 7.1.17
- **React Native Paper**: 5.14.5
- **Zustand**: 5.0.7 (управление состоянием)
- **MMKV**: 3.3.0 (локальное хранилище)
- **Expo Camera**: 16.1.11
- **Expo AV**: 15.1.7 (видео проигрывание)
- **Expo LibVLC Player**: 2.0.10 (RTSP потоки)
- **React Native Zoomable View**: 2.4.2 (зум и панорамирование)
- **Fast XML Parser**: 5.2.5 (парсинг XML)
- **React Native QR Image Reader**: 1.0.5 (чтение QR из галереи)
- **Lodash**: 4.17.21 (утилиты)

## 📋 Требования

- Node.js (версия 18 или выше)
- Yarn или npm
- Expo CLI
- iOS симулятор или Android эмулятор
- Физическое устройство для полной функциональности камеры

## 🚀 Установка и запуск

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd line_rn
   ```

2. **Установка зависимостей**
   ```bash
   yarn install
   # или
   npm install
   ```

3. **Запуск проекта**
   ```bash
   # Запуск в режиме разработки
   yarn start
   # или
   npm start

   # Запуск на Android
   yarn android
   # или
   npm run android

   # Запуск на iOS
   yarn ios
   # или
   npm run ios
   ```

## 📁 Структура проекта

```
src/
├── components/           # Переиспользуемые компоненты
│   ├── CreateServerModal.tsx
│   ├── EditServerModal.tsx
│   ├── EditRTSPModal.tsx     # Редактирование RTSP серверов
│   ├── RTSPModal.tsx         # Добавление RTSP серверов
│   ├── ServerCard.tsx
│   └── Timeline.tsx
├── navigation/           # Навигация
│   ├── AppNavigator.tsx
│   └── types.ts
├── screens/              # Экраны приложения
│   ├── CamerasScreen.tsx     # Просмотр камер
│   ├── HomeScreen.tsx        # Главный экран
│   ├── QRScannerScreen.tsx   # Сканирование QR
│   └── RTSPScreen.tsx        # Просмотр RTSP потоков
├── store/                # Управление состоянием
│   ├── serverStore.ts        # Серверы видеонаблюдения
│   └── rtspStore.ts          # RTSP серверы
├── themes/               # Темы оформления
│   └── darkTheme.ts
├── types/                # TypeScript типы
│   └── react-native-xml2js.d.ts
└── utils/                # Утилиты
    ├── cameraApi.ts          # API для работы с камерами
    └── qrParser.ts           # Парсинг QR-кодов
```

## 📖 Использование

### 1. Добавление сервера

#### Серверы видеонаблюдения:
- **QR-сканирование**: Нажмите на кнопку `+` → `Камера` и отсканируйте QR-код
- **Из галереи**: Нажмите на кнопку `+` → `Из галереи` и выберите изображение с QR-кодом
- **Вручную**: Нажмите на кнопку `+` → `Вручную` и введите данные сервера

#### RTSP потоки:
- **Прямое подключение**: Нажмите на кнопку `+` → `RTSP` и введите:
  - Название сервера (для удобства)
  - RTSP URL (например: `rtsp://192.168.1.100:554/stream`)
- **Поддерживаемые форматы**: H.264, H.265, MJPEG и другие форматы, поддерживаемые VLC

### 2. Формат QR-кода

QR-код должен содержать JSON с данными сервера:
```json
{
  "url": "https://your-server.com", // возможно без протокола
  "port": 443,
  "login": "username",
  "pass": "password"
}
```

### 3. Просмотр камер

#### Серверы видеонаблюдения:
- Выберите сервер из списка на главном экране
- Камеры отображаются в адаптивной сетке
- Нажмите на камеру для открытия полноэкранного режима
- Используйте жесты для зума и панорамирования

#### RTSP потоки:
- Выберите RTSP сервер из списка на главном экране
- Видеопоток открывается в полноэкранном режиме с использованием VLC плеера
- Поддержка зума и панорамирования с помощью жестов
- Автоматическое переподключение при потере соединения

### 4. Работа с архивом

- В полноэкранном режиме используйте timeline внизу экрана
- Переключайтесь между Live режимом и архивом
- Выбирайте временные интервалы для просмотра записей
- *Примечание: архив доступен только для серверов видеонаблюдения*

## 🔧 Настройки

Приложение автоматически сохраняет:
- Список серверов видеонаблюдения
- Список RTSP серверов
- Последний использованный сервер каждого типа
- Настройки подключения
- Время последнего использования каждого сервера

Данные хранятся локально с использованием MMKV для быстрого доступа и синхронизации между сессиями.

## 📱 Поддерживаемые платформы

- **iOS**: 12.0+
- **Android**: API 23+ (Android 6.0)

## 🔒 Разрешения

Приложение требует следующие разрешения:
- **Камера**: Для сканирования QR-кодов
- **Галерея**: Для выбора изображений с QR-кодами
- **Сеть**: Для подключения к серверам видеонаблюдения и RTSP потокам
- **Локальная сеть** (iOS): Для доступа к IP-камерам и серверам в локальной сети
- **Запись звука**: Может потребоваться для некоторых RTSP потоков (можно отключить)
- **Хранилище**: Для чтения изображений с QR-кодами из галереи

## 🐛 Отладка

При возникновении проблем:

### Общие проблемы:
1. Проверьте подключение к сети
2. Убедитесь в правильности данных сервера
3. Проверьте разрешения приложения
4. Просмотрите логи в Metro bundler

### Проблемы с RTSP потоками:
1. Проверьте корректность RTSP URL
2. Убедитесь, что камера поддерживает RTSP протокол
3. Проверьте, что порт не заблокирован брандмауэром
4. Попробуйте разные форматы URL:
   - `rtsp://ip:port/stream`
   - `rtsp://username:password@ip:port/stream`
   - `rtsp://ip:port/live/ch00_0`
5. Проверьте настройки камеры (TCP/UDP, кодеки)

### VLC плеер:
- Приложение использует VLC для воспроизведения RTSP потоков
- При проблемах с воспроизведением проверьте совместимость формата видео
- VLC поддерживает большинство современных кодеков (H.264, H.265, MJPEG)

## 🤝 Разработка

Для разработки:

1. Установите зависимости разработки
2. Используйте TypeScript для типизации
3. Тестируйте на реальных устройствах
4. При работе с RTSP потоками тестируйте на физических устройствах (эмуляторы могут не поддерживать VLC)
