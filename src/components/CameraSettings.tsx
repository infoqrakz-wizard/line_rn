import { View, Text, StyleSheet } from "react-native";
import React from "react";

const CameraSettings = () => {
  return (
    <View style={styles.container}>
      <Text>CameraSettings</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default CameraSettings;
